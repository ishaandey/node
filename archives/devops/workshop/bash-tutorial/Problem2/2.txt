
1. To rename things in Bash we can use `mv` (which stands for move).
The interesting thing is that we can "move" a file from one name to another.
- Try the following: mv anakin.txt darthvader.txt

2. There's a file called obiwan.txt, try deleting it.
- Try typing: rm obiwan.txt